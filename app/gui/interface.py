"""Streamlit UI entrypoints (placeholder).

Currently, `main.py` contains the concrete Streamlit page. This module exists
as a future refactor target if you want to split UI into smaller, testable
functions without changing behavior.
"""

from __future__ import annotations


def render_app() -> None:
    """Render the Streamlit application.

    Not implemented: `main.py` is the current entry point.
    """

    raise NotImplementedError("UI is currently implemented in `main.py`.")
